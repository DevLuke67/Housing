<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.studio.housing3">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".ViewSales"></activity>
        <activity android:name=".Editothers"></activity>
        <activity android:name=".Editcontact" />
        <activity
            android:name=".EditAbout"
            android:theme="@style/editabout"></activity>
        <activity
            android:name=".LoginRegister"
            android:theme="@style/register" />
        <activity
            android:name=".Login"
            android:theme="@style/register" />
        <activity
            android:name=".Register"
            android:theme="@style/register" />
        <activity android:name=".Rent" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>